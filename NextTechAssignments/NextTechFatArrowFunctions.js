function addFive(num) {
    return num + 5;
  }
  let addFive = (num) => {
    num + 5;
  }; 
  
  
  
  function divideBy(x, y) {
    return x / y;
  }
  let divideBy = (x, y) => {
    return x / y;
  };
  
  
  
  function logToConsole() {
    let msg = "MIT Coding Certificate";
    console.log(msg);
  }
  let logToConsole = () => {
    let msg = "MIT Coding Certificate";
    console.log(msg);
  };
  